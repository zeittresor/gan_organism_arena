GAN Organism Arena alpha22 build verification (2026-09-04)

Native user evidence: Alpha21 installation completed all eight steps, including
parse, self-test and smoke test. Supplied 2026-09-04 logs have no ERROR/WARNING
entries. Runtime samples show temporary high movement/contact costs and later
population decline. Screenshots alone cannot establish motion trajectories.

Alpha22 source execution with substitute engine objects:
NAVIGATION SELFTEST:  43  checks;  0  failures
POSTURE SELFTEST:  868  checks;  0  failures
SUPPORT SELFTEST:  857  checks;  0  failures
LOCOMOTION SELFTEST:  3371  checks;  0  failures
INTERACTION SELFTEST:  5443  checks;  0  failures
ECOLOGY SELFTEST:  51  checks;  0  failures
SURFACE SELFTEST:  71  checks;  0  failures
LIFE CYCLE SELFTEST:  115  checks;  0  failures
BIOLOGY SELFTEST:  617  checks;  0  failures
EXPERIMENT SELFTEST:  21  checks;  0  failures
SELFTEST OK: morphology_signatures= 7  total_cells= 1548  crossover_family= 99  viable= 0.92  unstable= 0.0  thought= Approach carefully, then take the resource.
All 11457 named assertions and the core test passed.

Navigation cases include three body plans reaching bottom food at the head,
actual displacement, finite feeding range, persistent exploration, no-progress
recovery, stage-aware food selection, sensing range, partner approach before
courtship, and respiration taking priority over partner seeking.

Production coupling/gestation/birth/development integration passes, with one
offspring and bounded energy/capacity. Production OBJ regression passes natural,
cell, energy and natural view transitions without graphics readback. Fifteen
Python adapter unit tests pass. The unchanged active protocol transport was not
re-run end-to-end in this iteration; observation serialization is covered by
the production experiment self-test.

47 GDScripts grammar checked, all registered for native parsing and package
verification. 55 settings/actions retain EN/DE/FR labels/tooltips. Navigation
status and partner-seeking labels are also provided in all three languages.

Native Alpha22 Windows/Godot execution, GPU output, TTS and FPS were NOT tested
here. The Python source harness does not model Godot's complete type system,
rendering server or runtime behavior. Windows installer gates remain active.

Archive gate verifies fresh extraction, ZIP CRC, all payload SHA-256 entries
and file count. Personal settings, generated logs and temporary data excluded.
